sortme = function (names) {
  return names.sort(); // Using the built-in sort() function which sorts strings lexicographically by default
};

// names.sort(): The sort() method sorts the array in place, using the default string comparison (lexicographical order). This method handles alphabetic sorting automatically.
