function solution(str, ending) {
  // Use the endsWith() method to check if 'str' ends with the 'ending' string
  return str.endsWith(ending);

  // endsWith(): This built-in method checks if the string (str) ends with the substring (ending). It returns true if it does, and false otherwise.
}
