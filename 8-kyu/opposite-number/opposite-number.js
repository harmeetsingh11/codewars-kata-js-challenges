function findOpposite(num) {
  // Simply negate the input number to get its additive inverse
  return -num;
}

/*
Additive Inverse: The additive inverse of a number is what you add to it to get zero. For any number n, the additive inverse is -n.
Negation: Using the unary - operator in JavaScript efficiently calculates the additive inverse.
*/
