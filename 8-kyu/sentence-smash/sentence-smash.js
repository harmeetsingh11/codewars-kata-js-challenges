function smash(words) {
  // The argument `' '` specifies that a space should be placed between each word when joining them.
  // This effectively concatenates all the elements in the `words` array into a single string with spaces separating them.
  return words.join(' ');
}
