function contains(list, item) {
  // Use the Array.prototype.includes() method to check for item existence.
  // This method is ideal because it is built into JavaScript, making it both efficient and concise.
  // It checks if the item exists in the array and returns a boolean (true if found, false otherwise).
  return list.includes(item);
}
