function twiceAsOld(fatherAge, sonAge) {
  // Calculate the absolute difference between father's age and twice the son's age
  return Math.abs(fatherAge - 2 * sonAge);
}

// Use Math.abs() to ensure the result is always non-negative (absolute value).
